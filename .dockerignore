# The Lambda image packages only the prebuilt binary (compiled on the CI runner
# — see .github/workflows/deploy-launch-control.yaml). Exclude everything, then
# re-include just that one binary and its ancestor dirs, so the build context
# stays tiny.
*
!target
target/*
!target/release
target/release/*
!target/release/launch-control-server
