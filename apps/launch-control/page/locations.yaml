# Page: locations — framework binder: grid on top; selecting a row reveals a
# Details form plus one tab per declared relation.
title: Locations
body:
  - kind: binder
    name: locations
    observe: locations
