# Page: feed — the raw incoming stream as a full table (framework grammar;
# crud = the full table chrome, same widget upstream's tableview drove).
title: Live Feed
body:
  - kind: crud
    table: updates
    params:
      columns: [updated, region, delta]
      id_column: region
