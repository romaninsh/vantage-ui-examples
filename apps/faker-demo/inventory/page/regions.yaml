# Page: regions — the full per-region table, live (framework grammar; the
# hosted grid chrome sorts by header click). Publishes the selected region.
title: Regions
body:
  - kind: crud
    name: regions
    table: top
    params:
      columns: [region, visitors, vs_baseline, live]
      id_column: region
