# dev profile → `profile.motd`; SECRET_ keys land in `secret.*` (readable in
# scope for datasource use, never rendered by the demo page).
MOTD=dev profile — sim data, refreshed live
SECRET_API_KEY=dev-key-not-a-real-secret
