# yaml-language-server: $schema=./page-schema-1.json
title: Bakeries
body:
  - kind: binder
    name: bakeries
    observe: bakery-surreal/bakery
    params:
      columns:
        name: { label: Shop, pin: true }
        profit_margin: { label: "Margin %" }
