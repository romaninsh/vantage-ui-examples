# yaml-language-server: $schema=./page-schema-1.json
#
# The app's own README, rendered. `!include` splices the file in as the
# template's text, so this page and the repository never say different
# things.
title: Welcome
body:
  - kind: column
    children:
      # The setup wizard opens from the Products page toolbar for now:
      # a standalone `kind: button` runs in the scope-only ScriptEnv,
      # which has no `actions` namespace — only grid toolbar/row
      # bodies dispatch actions today. Move the entry point here once
      # buttons can dispatch.
      - kind: markdown
        text: !include ../README.md
