# yaml-language-server: $schema=./page-schema-1.json
# Page: replicasets — a binder. Selecting a ReplicaSet reveals its Pods tab.
title: ReplicaSets
body:
  - kind: binder
    name: replicasets
    table: replicasets
