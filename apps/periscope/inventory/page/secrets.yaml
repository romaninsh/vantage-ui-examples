# yaml-language-server: $schema=./page-schema-1.json
# Page: secrets — flat list of Secrets (metadata only; no values are fetched).
title: Secrets

elements:
  - kind: tableview
    spot: body
    table: secrets
