# Datasource ll2c — client-filter REST (for the launch↔payload join, whose
# filter the API ignores). Same fake server; Vantage narrows in memory on the
# row's nested launch.id.

description: "Mock Launch Library 2 (client filtered)."
type: api-client
url: "http://127.0.0.1:14599"
response_key: results
pagination:
  page_param: offset
  limit_param: limit
  offset_based: true
filter_strategy: client
