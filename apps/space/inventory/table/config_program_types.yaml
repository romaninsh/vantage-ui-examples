# yaml-language-server: $schema=./table-schema-1.json

# Table: config_program_types — lookup table for program_types.

datasource: ll2
title: program_types
table: "config/program_types/"

columns:
  - { name: id, type: int, flags: [id] }
  - { name: name, type: string, flags: [title, searchable] }
