# yaml-language-server: $schema=./table-schema-1.json

# Table: config_mission_types — lookup table for mission_types.

datasource: ll2
title: mission_types
table: "config/mission_types/"

columns:
  - { name: id, type: int, flags: [id] }
  - { name: name, type: string, flags: [title, searchable] }
