# yaml-language-server: $schema=./page-schema-1.json

# Page: starship_vehicles — Starship/Super Heavy prototype fleet, sorted by
# most recent flight.

title: Starship fleet

body:
  - kind: crud
    table: { script: 'scenery("starship_vehicles").sort("last_launch_date", "desc")' }
