# yaml-language-server: $schema=./page-schema-1.json

# Page: config_landing_types — list view.

title: landing types

elements:
  - kind: tableview
    spot: body
    table: config_landing_types
    params: {}
