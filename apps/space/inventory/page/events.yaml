# yaml-language-server: $schema=./page-schema-1.json

# Page: events — list view (standalone). Sorted by date, most recent first.

title: Events

body:
  - kind: crud
    table: { script: 'scenery("events").sort("date", "desc")' }
