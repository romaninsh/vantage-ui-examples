# yaml-language-server: $schema=./page-schema-1.json

# Page: config_timeline_event_types — list view.

title: timeline event types

body:
  - kind: crud
    table: config_timeline_event_types
    params: {}
