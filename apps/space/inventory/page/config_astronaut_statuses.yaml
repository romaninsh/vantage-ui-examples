# yaml-language-server: $schema=./page-schema-1.json

# Page: config_astronaut_statuses — list view.

title: astronaut statuses

body:
  - kind: crud
    table: config_astronaut_statuses
    params: {}
