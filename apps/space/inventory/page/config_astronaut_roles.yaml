# yaml-language-server: $schema=./page-schema-1.json

# Page: config_astronaut_roles — list view.

title: astronaut roles

elements:
  - kind: tableview
    spot: body
    table: config_astronaut_roles
    params: {}
