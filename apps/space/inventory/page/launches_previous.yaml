# yaml-language-server: $schema=./page-schema-1.json

# Page: launches_previous — past launches, most recent first.

title: Previous launches

body:
  - kind: crud
    table: { script: 'scenery("launches_previous").sort("net", "desc")' }
