# yaml-language-server: $schema=./page-schema-1.json

# Page: config_spacecraft_statuses — list view.

title: spacecraft statuses

body:
  - kind: crud
    table: config_spacecraft_statuses
    params: {}
