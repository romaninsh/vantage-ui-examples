# yaml-language-server: $schema=./page-schema-1.json

# Page: spacecraft_configuration_families — list view.

title: Spacecraft families

elements:
  - kind: tableview
    spot: body
    table: spacecraft_configuration_families
    params:
      default_sort: { column: name, direction: asc }
