# yaml-language-server: $schema=./page-schema-1.json

# Page: docking_events — list view. Right-click a space station → "Open
# dockings" narrows by space_station_target__id.

title: Docking events

body:
  - kind: crud
    table: { script: 'scenery("docking_events").sort("docking", "desc")' }
