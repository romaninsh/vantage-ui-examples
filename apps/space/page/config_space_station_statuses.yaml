# yaml-language-server: $schema=./page-schema-1.json

# Page: config_space_station_statuses — list view.

title: space station statuses

body:
  - kind: crud
    table: config_space_station_statuses
    params: {}
