# yaml-language-server: $schema=./page-schema-1.json

# Page: config_road_closure_statuses — list view.

title: road closure statuses

body:
  - kind: crud
    table: config_road_closure_statuses
    params: {}
