# yaml-language-server: $schema=./page-schema-1.json
# Page: launcher_configuration_families — the family tree rocket designs
# belong to.
title: Launcher families
body:
  - kind: binder
    name: launcher_configuration_families
    observe: launcher_configuration_families
    params:
      columns:
        name: { label: Family, pin: true, width: 240 }
        parent.name: { label: Parent }
