# yaml-language-server: $schema=./page-schema-1.json
# Page: spacewalks — extravehicular activities.
title: Spacewalks
body:
  - kind: binder
    name: spacewalks
    observe: spacewalks
    params:
      columns:
        name: { label: Spacewalk, pin: true, width: 300 }
        spacestation.name: { label: Station }
        expedition.name: { label: Expedition }
        start: { label: Start, time_precision: minutes }
        duration: { label: Duration }
        location: { label: Location }
