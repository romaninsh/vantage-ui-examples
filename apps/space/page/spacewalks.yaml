# yaml-language-server: $schema=./page-schema-1.json

# Page: spacewalks — list view. Sorted by start date, most recent first.

title: Spacewalks

body:
  - kind: crud
    table: { script: 'scenery("spacewalks").sort("start", "desc")' }
