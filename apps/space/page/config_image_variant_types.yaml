# yaml-language-server: $schema=./page-schema-1.json

# Page: config_image_variant_types — list view.

title: image variant types

body:
  - kind: crud
    table: config_image_variant_types
    params: {}
