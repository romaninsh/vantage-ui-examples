# yaml-language-server: $schema=./page-schema-1.json

# Page: expeditions — list view. Right-click a space station → "Open
# expeditions" narrows by space_station.

title: Expeditions

body:
  - kind: crud
    table: { script: 'scenery("expeditions").sort("start", "desc")' }
