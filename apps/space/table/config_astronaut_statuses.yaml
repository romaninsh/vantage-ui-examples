# yaml-language-server: $schema=./table-schema-1.json

# Table: config_astronaut_statuses — lookup table for astronaut_statuses.

datasource: ll2
title: astronaut_statuses
table: "config/astronaut_statuses/"

columns:
  - { name: id, type: int, flags: [id] }
  - { name: name, type: string, flags: [title, searchable] }
